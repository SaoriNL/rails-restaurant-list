<div class='container'>
  <h1>all the restaurants</h1>
  <% @restaurants.each do |restaurant| %>
    <h3><%= restaurant.name %></h3>
    <h3><%= restaurant.address %></h3>
    <h3><%= restaurant.category_id %></h3>
    <%# <h3><%= link_to restaurant_path %>
  <% end %>
</div>
